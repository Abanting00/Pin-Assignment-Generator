{
  "name": "pin-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node index.js",
    "heroku-postbuild": "concurrently \"cd backend && npm install && node index.js\" \" d pin-assignment && npm install && npm run build\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
